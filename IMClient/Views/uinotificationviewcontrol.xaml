<UserControl x:Class="IMClient.UINotificationViewControl" x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:bcl="http://schemas.intermech.org/bcl/wpf"
             xmlns:local="clr-namespace:IMClient"
             Margin="0" Padding="0" SnapsToDevicePixels="true" FontSize="{Binding FontSize}">
  <FrameworkElement.Resources>
    <local:UINotificationIconToImageSourceConverter x:Key="UINotificationIconToImage"/>
    <local:UINotificationsItemToInlinesConverter x:Key="MessageToInlines"/>
    <bcl:RelativeSizeConverter x:Key="SmallerFont" Ratio="0.8"/>
  </FrameworkElement.Resources>
  <ListView x:Name="ItemsListView" Margin="0" Padding="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ItemsSource="{Binding Items}">
    <ItemsControl.ItemContainerStyle>
      <Style TargetType="{x:Type ListViewItem}">
        <Setter Property="Control.HorizontalContentAlignment" Value="Stretch"/>
      </Style>
    </ItemsControl.ItemContainerStyle>
    <ItemsControl.ItemTemplate>
      <DataTemplate>
        <Border BorderBrush="{x:Static JetSystemColors.ControlBrush}" BorderThickness="1">
          <Grid Margin="2">
            <Grid.RowDefinitions>
              <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <UIElement.InputBindings>
              <MouseBinding Gesture="LeftDoubleClick"
                            Command="{Binding DataContext.FireItemActionCommand, ElementName=ItemsListView, Mode=OneWay}"
                            CommandParameter="{Binding}"/>
            </UIElement.InputBindings>
            <Image Grid.Column="0" Grid.Row="0" Stretch="None" VerticalAlignment="Top" Margin="0,0,8,0"
                   Source="{Binding Icon, Converter={StaticResource UINotificationIconToImage}}"/>
            <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,8,0">
              <DockPanel Margin="0,0,0,4" LastChildFill="true">
                <TextBlock DockPanel.Dock="Right" FontStyle="Italic" VerticalAlignment="Center"
                           Text="{Binding DateTime, StringFormat=g}"
                           FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource SmallerFont}}"/>
                <TextBlock FontWeight="Bold" Text="{Binding Caption}"/>
              </DockPanel>
              <TextBlock TextWrapping="WrapWithOverflow" TextTrimming="CharacterEllipsis" MaxHeight="100"
                         bcl:TextBlockExtender.Inlines="{Binding Path=., Mode=OneWay, Converter={StaticResource MessageToInlines}}"/>
            </StackPanel>
            <Button Grid.Column="2" Grid.Row="0" BorderThickness="0" Padding="0" VerticalAlignment="Top"
                    Command="{Binding DataContext.CloseItemCommand, ElementName=ItemsListView, Mode=OneWay}"
                    CommandParameter="{Binding}">
              <ContentControl.Content>
                <Image Source="{x:Static local:UINotificationsResources.CloseButton}"/>
              </ContentControl.Content>
            </Button>
          </Grid>
        </Border>
      </DataTemplate>
    </ItemsControl.ItemTemplate>
  </ListView>
</UserControl>